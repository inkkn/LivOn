global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus self-metrics
  # - job_name: prometheus
  #   static_configs:
  #     - targets:
  #         - prometheus:9090

  # OTEL Collector metrics (THIS is your app metrics)
  - job_name: otel-collector
    static_configs:
      - targets:
          - otel-collector:8889
